{
  "name": "design-system-phase2",
  "version": "1.0.0",
  "description": "Design System Phase 2 - Component naming and tokens",
  "scripts": {
    "dev": "python -m http.server 8000",
    "build": "echo 'Building design system...' && cp -r design-system/* public/",
    "demo": "echo 'Starting demo server...' && python -m http.server 8000"
  },
  "devDependencies": {
    "sass": "^1.69.0"
  },
  "dependencies": {
    "@line/bot-sdk": "^10.0.0",
    "dotenv": "^17.2.1",
    "express": "^5.1.0"
  }
}
